version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 22
    commands:
      - npm install
  build:
    commands:
      - npm run build
      - mkdir -p artifact
      - cp -R .next public package.json package-lock.json artifact/
artifacts:
  files:
    - artifact/**/*
